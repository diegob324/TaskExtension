# TaskExtension
A simple extension for chrome where the user can create a task list.
The user will be alerted about these tasks, if incomplete, through system notifications.
Can change icon.png for another image if you wish.
